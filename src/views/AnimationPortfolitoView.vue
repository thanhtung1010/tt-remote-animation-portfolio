<template>
  <div id="animation-vue-app">
    vue app
  </div>
</template>

<script lang="ts">
  import * as THREE from 'three';
import { defineComponent } from "vue";
// import HelloWorld from "@/components/HelloWorld.vue"; // @ is an alias to /src

// export default defineComponent({
//   name: "AnimationPortfolitoView",
//   components: {
//     HelloWorld,
//   },
// });
  // const scene = new THREE.Scene();
  // const camera = new THREE.PerspectiveCamera( 75, window.innerWidth / window.innerHeight, 0.1, 1000 );

  // const renderer = new THREE.WebGLRenderer();
  // const animationElement: HTMLElement | null = document.getElementById('animation-vue-app');
  
  // if (animationElement) {
  //   renderer.setSize( animationElement.offsetWidth, animationElement.offsetHeight );
  //   animationElement.appendChild(renderer.domElement);
  // }
  // document.get.appendChild( renderer.domElement );

  export default defineComponent({name: "AnimationPortfolitoView"});
</script>
